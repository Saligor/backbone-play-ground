/**
 *
 *
 */

// globals Backbone;

var gen = gen || {};

(function ($, gen, bb) {

    gen.models = gen.models || {};

    gen.models.testComponent = bb.Model.extend({

    });
}(jQuery, gen, Backbone));